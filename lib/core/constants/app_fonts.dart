/// Centralized font family name constants.
class AppFonts {
  AppFonts._();

  /// Serif header font — Italiana (Google Fonts).
  static const String serif = 'Italiana';

  /// Body sans-serif font — DM Sans (Google Fonts).
  static const String sans = 'DM Sans';
}
